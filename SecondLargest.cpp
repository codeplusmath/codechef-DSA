#include <iostream>
#include <vector>
using namespace std;

int Sol(){
    int x;
    int result[3];
    cin>>result[0]>>endl;
    cin>>x>>endl;
    if(x)
}

int main(){
    int result;
    result = Sol();
    cout<<result;
    return 0;
}

static const int _ = [](){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return 0;
}
